.box
  = form_for(resource_name, resource, :url => session_path(resource_name)) do |f|
    %p
      = f.label :name
      %br
      = f.text_field :name

    %p
      = f.label :password
      %br
      = f.password_field :password

    %p 
      = f.submit "Sign in"



