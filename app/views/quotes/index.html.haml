- @quotes.each do |quote|
  .quote
    .title
      = link_to '№ ' + quote.id.to_s, quote 
      approved by anti, 
      - if quote.created_at
        = quote.created_at.strftime("%d.%m.%Y %H:%M")
    .content= quote.content

